# macOS
.DS_Store
._*

# Reference materials (not part of the live site)
BetterMeCalistenicsFlow/
ReferencesforWebsite:MobileAppStyle/
namecheap/

# Server-generated files
waitlist.csv

# Editor
.vscode/
.idea/
*.swp
*.swo

# Env / secrets (never commit)
.env
.env.local
.env.*.local

# Generated artifacts
scripts/video-manifest.json
scripts/node_modules/
scripts/package-lock.json
scripts/upload.log

# ML / CV model weights — not source, downloaded on-demand by the
# scripts that need them (Ultralytics YOLO, etc).
*.pt
*.onnx
*.weights
.claude/

# Build artifacts (root-level vite dist; react-app/dist already ignored
# in react-app/.gitignore).
dist/

# Local backup files written by data-sync scripts as safety nets.
# Pattern matches libo-data.js.bak_*, workouts.json.bak_*, etc.
*.bak_*
